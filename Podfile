# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GithubUserFinder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubUserFinder
  pod 'Alamofire', '~> 5.0.0-rc.2'
  pod 'AlamofireImage'

  target 'GithubUserFinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubUserFinderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
